# PostSharp.Backstage.Tools

The `PostSharp.Backstage.Tools` package is not meant to be referenced in user projects.

It carries the Backstage tools of PostSharp as zipped executables: the worker, which serves the local setup pages and uploads telemetry, and the Windows notifier, which shows toast notifications. PostSharp extracts them on the machine of the user when they are needed.
